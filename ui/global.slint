//! global styles
export global ROOT_GLOBAL {
  in-out property <length> window-height : 660px;
  in-out property <length> window-width : 1080px;
  in-out property <length> font-size : 16px;
  in-out property <length> padding : 0px;
  in-out property <brush> theme-color-list: #2B2D31;
  in-out property <brush> theme-color-chat: #313338;
}